<Window x:Class="WpfActivationClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Активация" MinWidth="300" MinHeight="150" Width="400" Height="200">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Введите лицензионный ключ:" FontSize="14" 
                   HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <TextBox x:Name="LicenseKeyTextBox" Grid.Row="1" Margin="0,5,0,0" 
                 Height="25" HorizontalAlignment="Stretch"/>

        <StackPanel Grid.Row="2" Margin="0,10,0,0" HorizontalAlignment="Stretch">
            <Button Content="Активировать" Height="30" Click="ActivateButton_Click" 
                    HorizontalAlignment="Stretch"/>
            <TextBlock x:Name="ResultTextBlock" Margin="0,10,0,0" FontSize="12" 
                       Foreground="Red" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
